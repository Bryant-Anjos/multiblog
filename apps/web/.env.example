# Server-side API URL (used by Next.js server components / route handlers)
API_URL=http://localhost:8080
# Browser-side API URL (used by the client / restore; must be reachable from the browser)
NEXT_PUBLIC_API_URL=http://localhost:8080
